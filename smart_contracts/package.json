{
  "name": "smart_contracts",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "blockchain": "npx ganache-cli --deterministic",
    "compile": "npx truffle compile",
    "migrate:development": "npx truffle migrate --network development"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^3.4.1"
  },
  "devDependencies": {
    "ganache-cli": "^6.12.2",
    "truffle": "^5.2.3"
  }
}
